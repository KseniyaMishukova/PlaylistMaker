<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.practicum.playlistmaker">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaylistMaker.Main"
        tools:targetApi="31">

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.search.SearchActivity"
            android:theme="@style/Theme.PlaylistMaker.Search" />

        <activity android:name=".presentation.media.MediaLibraryActivity" />

        <activity
            android:name=".presentation.settings.SettingsActivity"
            android:theme="@style/Theme.PlaylistMaker.Settings" />

        <activity
            android:name=".presentation.audio.AudioPlayerActivity"
            android:theme="@style/Theme.PlaylistMaker.Search"
            android:exported="false" />

    </application>
</manifest>